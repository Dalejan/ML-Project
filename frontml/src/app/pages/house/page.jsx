import React, { Fragment } from "react";
import Formulario from "../../components/form/form";

function page(props) {
    return (
        <Fragment>
            <div>
                <Formulario />;
            </div>
        </Fragment>
    );
}
export default page;