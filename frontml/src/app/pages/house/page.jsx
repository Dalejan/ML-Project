import React, { Fragment } from "react";
import Formulario from "../../components/form/form";

function page(props) {
    return (
        <Fragment>
           
                <Formulario />;
        </Fragment>
            );
}
            export default page;